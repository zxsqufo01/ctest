class Solution {
    public void moveZeroes(int[] nums) {
        // if(nums == null || nums.length == 0) return 0;
        int index = 0;
        for(int num : nums) if(num!=0) nums[index++] = num;
        for(;index<nums.length;) nums[index++] = 0;
    }
}
