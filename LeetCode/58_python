class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        if s == '':
            return 0
        length = 0
        for i in s[::-1].strip():
            if i != ' ':
                length += 1
            else:
                return length
        return length
